repos:
  - repo: local
    hooks:
      - id: kvt-precommit
        name: make precommit (ruff format + lint, biome fix)
        entry: make precommit
        language: system
        pass_filenames: false
        types_or: [python]
