# secrets
.env*
!.env.example
.rev-ok
.desloppify/
.diffler/

# recordings and datasets (personal data, local only)
data/
docs/
*.mp4
*.mkv
*.webm
*.png
*.jpg
*.jpeg
exports/

# python
__pycache__/
.venv/
.mypy_cache
.pytest_cache
.ruff_cache
.coverage*
*.egg-info
dist/
build/
.vulture

# node
node_modules/

# editor / os
.DS_Store
.idea/
.vscode/
*.swp

# tooling local state
.playwright-mcp/

# weights are distributed on their own, never through git; the CC-BY keybed mesh ships here
# because the generator page needs it to render at all
web/public/*.onnx
web/public/*.onnx.data
!web/public/models/
