# keyboard-vis-tracking

Browser-only spike: live camera keyboard and hand tracking with keys-light-up visuals, built to land in kinesthesia.

## Commands

- `make install`
- `make dev`
- `make quality`
- `make help` lists the rest.
